{% macro render(text, options = {}) %}
    <div class="hc-helper {{options.class}}">
        <span class="hc-helper__text" {% if options.id %} id="{{options.id}}" {% endif %}>{{text}}</span>
    </div>
{% endmacro %}